name: mechamatch-backend
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn
      - pydantic
      - pytest-cov
      - -e ../shared
